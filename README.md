# port-killer
A small Windows utility that runs a script only if it’s not already active. Includes a BAT checker and a screenshot demo.A utility designed to ensure single-instance execution of your Windows scripts using a BAT-based process checker.
